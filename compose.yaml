services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=rennes-tech'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=rennes-tech'
    ports:
      - '5432:5432'
